---
name: xhs-ai-ip-content
description: 基于用户原始材料或现有文稿，生产、修改或只读 Review 小红书 AI、Agent、FDE 和数据化转型内容；用于标题、正文、标签、内容结构、事实与作者口吻、证据强度、概念一致性，以及句、段、篇、跨页和上下左右阅读依赖的表达逻辑。普通写作默认只交付用户要求的文字，不自动拆页或创建流程状态；用户明确要求逐页内容、提示词或图片时转交 xhs-carousel-image-production。
---

# 小红书内容生产与表达逻辑

## 核心原则

保证任何判断依赖的前提，在判断之前已经出现，或在同一个完整阅读团块中可以直接获得。

不要把这个原则写成固定叙事模板。观点、案例、数据、过程或矛盾都可以先行；具体顺序由材料和读者理解路径决定。不要为了标题长度、段数、页数或视觉整齐删掉对象、条件、因果、边界和责任关系。

处理任何实质性生成、修改或 Review 前，完整读取 [references/expression-logic.md](references/expression-logic.md)。该文件定义事实边界、分层逻辑检查、硬阻断项、回修方法和正反例。

## 选择任务路径

先按用户本轮请求确定工作范围：

- **生成**：只生成用户要求的标题、正文、标签或其他文字成品。不要自动拆页、补齐未要求的产物或建立状态文件。
- **修改**：先恢复当前全部有效材料和作者意图，再整体重写最小完整受影响单元。基础判断或结构改变时，扩大重写范围，直到新旧逻辑不再并存。
- **Review**：只读诊断，不修改文件、不生成替代稿，除非用户同时明确要求修改。
- **逐页或视觉生产**：只有用户明确要求拆页、逐页内容、提示词、HTML 或图片时，才转交 `xhs-carousel-image-production`；不要在本技能里预设页数或页面组件。

用户只要求其中一项时，不扩展成完整生产流程。

## 恢复材料边界

优先读取用户本轮材料、用户指定文件、当前选题已有主工作文档和真实 `adjustment-log.md`。文件名不是前置条件；不要为了流程完整创建 `source-brief.md`、`draft.md` 或其他文档。

写作前在内部确认：

- 当前主要对象和读者正在处理的任务；
- 作者已经明确表达的判断；
- 支撑判断的事实、案例、来源或过程；
- 判断成立的条件、边界和证据强度；
- 尚未确认、不能代替作者决定的问题。

这些是推理检查项，不是固定输出字段。区分用户事实、公开来源、作者推测和待确认内容。没有真实案例时，写成观点推演、公开案例分析或待验证假设，不补写成作者实践。

在开始措辞前，先在内部划清本篇的**可表达主张边界**：哪些是材料事实，哪些是作者已确认的判断，哪些只能作为编辑分析或问题提出，哪些没有依据不能写。对每个准备写入的判断，同时识别它明说的内容和它隐含的前提、比较基线、时间状态、因果、责任归属或适用范围。让每一项都能回溯到材料、作者已确认立场，或同一／更早阅读团块中已经建立的内容。当前写作意图只决定选题、角度或形式，不能单独支持新增事实、因果、范围或作者立场；“升华”“更有力量”等表达要求也只改变写法，不自动扩大可表达主张边界。

这不是发布前才执行的检查，也不是关键词黑名单。它是起草时的生成约束：一旦一句话新增了未被材料支持的关系，立即补足前提、降为明确的分析或问题，或改写删除该关系；不要为制造“人的口吻”虚构作者的过去状态、认识变化或经历。内部完成这一步即可，不把主张边界变成读者可见的固定栏目。

用户给出的“我想表达”“我更想用”“请升华成”“把它写成一个判断”等候选句，需要与材料和作者立场分开判断。这些话描述的是本轮文案请求，不等于作者已经确认其中新增的关系。只有用户明确说明这是自己的独立、待验证判断，即使来源不能证明也要保留时，才作为作者立场处理。其余候选句没有新增对象、条件、因果、范围或衡量标准的，可以直接采用；新增了却无材料支持的，不是论据。此时不要用近义词、降调或换成问句保留同一个未被支持的关系，改写为材料已经成立的判断。材料事实与明确作者立场优先于候选措辞的力度。

例如，材料只支持“本体在真实场景中带回物理数据，这些数据可能帮助智能能力训练和修正”时，不能把它写成“判断聚焦要看所有投入能否为下一轮带回信息”。后者新增了衡量标准。即使用户要求把它写得更有力量，也应回到材料中的判断，例如区分“真实数据是否改善目标任务”和“团队能否承受当前做法”，而不是替候选句补证据。

来源导读中，遇到这种不成立的候选核心句时，不把任务理解成“把这句话写得更好”，而是直接从材料中选择最接近、但已经成立的核心判断来完成成品。不要输出拒绝说明，也不要保留原句、其同义改写或同一衡量标准；用材料里的对象、动作、矛盾和可验证关系重写。

## 生成内容

1. 从可表达主张边界中选择自然的进入方式和展开路径，不套用统一开场、提问、编号框架或结尾。
2. 写每个句子或阅读团块时，当场闭合“明示主张 → 隐含依赖 → 证据／归属”。不要先按口吻生成，再在下游补查依据。
3. 让零上下文读者在第一完整阅读团块内识别正在讨论的对象、任务或条件。可以先给观点，但相邻内容必须立即补足理解该观点所需的具体上下文。
4. 按实际阅读顺序检查每个判断及其隐含前提是否已经可得；发现缺口时回到最早缺失前提的位置重写。
5. 保留作者表达复杂判断所需的长度。短文字只承担命名、导航、对照锚点或已经被证明的结论。
6. 只交付本轮要求的成品。标题数量、段落数量、正文结构和结尾形式均服从内容，不做机械补齐。

完成后执行一次阅读依赖冷读：逐个阅读团块确认“读者此刻知道什么、这一步凭什么成立、下一步为什么从这里出现”。

## 修改内容

先从当前全部有效材料恢复对象、判断、证据、边界和关系，再定位最早出错层：术语、句子、段落、整篇、跨页或视觉阅读顺序。

不要用追加桥句、“补充说明”或局部换词覆盖已经变化的基础判断。重写能够独立恢复逻辑的最小完整单元；若对象、中心判断、证据关系或一句话依赖的时间／比较／因果／作者立场改变，应同步重写整个受影响阅读团块及下游内容。除非用户明确要求压缩或删除，否则保留真实材料、动作、输入输出、因果、替代路径、适用条件和失败边界。

## Review 内容

Review 保持只读。按实际阅读顺序定位问题，每条诊断使用：

`位置 → 读者当前理解 → 缺失前提或错误关系 → 可能误读 → 最早回修层`

只报告会影响事实、作者立场、证据强度、概念角色或理解路径的问题。标题长度、段数、页数、是否有标题、是否使用问句、是否有总结页和布局偏好不作为缺陷。

用户明确要求 Review 或发布级成品时，在可使用子 Agent 的环境中增加一次隔离的独立语义审阅：只提供原始材料与待审成品，不提供预期答案、既有诊断或修改意图。独立审阅发现的问题仍按上述格式核对；不能由原始材料证明的问题不升级为结论。

## 写入与交付边界

- 用户只要求 Review 时不写文件。
- 用户指定文件时写入该文件；未指定时优先直接交付，不自行扩展文档体系。
- 不创建或维护生产状态、阶段标记、失效标记或人工阻断文件。
- 不调用图片生成工具，不创建 HTML 或 PNG。
- 文案修改会使已有图片文字不同步时，明确提醒用户，但不创建状态记录。
- 不把某一次成功的标题、比喻、文章结构或页面形式固化成默认模板。

## 通过门槛

- 判断忠于可追溯材料，没有虚构作者经历、认识变化、案例、事实或结果。
- 编辑推导没有被写成作者第一人称结论。
- 每个判断及其隐含的时间状态、比较基线、因果、责任归属和范围，都在起草时拥有可追溯的依据；没有用“人味”或修辞补出未出现的前史。
- 读者不需要作者脑内信息、创作讨论或未展示材料来补齐前提。
- 句、段、篇及跨页关系连续，因果、转折、对照和责任关系成立。
- 案例、数据和来源能够支持对应强度的判断，并保留适用边界。
- 技术对象由真实任务引出，模型、Agent、Tool、API、服务和人的职责没有错配。
- 自由形式不会因为标题、段数、页数或组件缺失被误判为不合格。
