---
name: xhs-ai-ip-content
description: 在 XHS 内容工作区，基于用户原始材料或既有文稿生成、修改或只读审阅面向外部读者的中文内容；适用于选题、标题、正文、读后感、公开分享、图文拆页和 imagegen 提示词。用户明确要求逐页可见文案或提示词时在本技能内处理；普通文字请求不自动拆页、写提示词或实际出图。
---

# 外部中文内容生产

遵守当前工作区 `AGENTS.md` 的账号立场和对外内容底线。本 Skill 不另建一套账号品味；它只提供在有材料、有改稿或有图文页任务时所需的方法。

## 选择任务路径

- **生成**：只产出用户要求的标题、正文、标签或其他文字。涉及来源、案例、作者判断或候选核心句时，读取 [references/material-boundaries.md](references/material-boundaries.md)。
- **修改或 Review**：读取 [references/revision-and-review.md](references/revision-and-review.md)。修改时整体重写最小完整受影响单元；Review 保持只读，除非用户同时要求改稿。
- **技术内容**：当模型、Agent、Tool、API、Graph、Loop、Eval、Trace 或人的职责承担推理任务时，读取 [references/technical-concepts.md](references/technical-concepts.md)。
- **图文页与提示词**：只有用户明确要求拆页、逐页可见文案、页面阅读顺序 Review 或 imagegen 提示词时，读取 [references/carousel-prompts.md](references/carousel-prompts.md)。需要材料边界或改稿方法时，同时读取对应 reference。

不要因为内容属于小红书，就自动拆页、补齐文件、创建状态记录或实际出图。

## 处理材料

1. 先恢复当前有效材料中的对象、作者已确认判断、证据、条件、边界与未解问题；文件名和流程文档不是前置条件。
2. 在措辞前确认每个准备写入的判断，是否能回溯到材料、明确作者立场或已展示上下文。用户的写作意图只决定表达任务，不单独增加可表达的事实或结论。
3. 选择适合材料的进入方式与结构。允许观点、案例、数据、过程或矛盾先行，不套固定开场、页数、标题或结尾。
4. 按读者实际顺序冷读成品。发现对象、前提、证据、责任或关系在后面才出现时，回到最早缺失处重写。

## 图文页与 imagegen

先让正文和判断稳定，再把连续内容拆为页面。每页交付按实际可见顺序排列的完整文字，以及可独立使用、逐字列出可见文字的 imagegen 提示词。先复制页面可见文字到提示词，再逐字符核对文字、数字、标点和引号；不要凭视觉相近的符号重写。

用户明确要求实际生成或编辑图片时，在页面文案和提示词稳定后调用 `imagegen`。不要在本 Skill 中默认创建 HTML、PNG、排版渲染或旧图文页 Skill 的资产。

## 交付边界

- 用户只要求 Review 时，只读诊断，不写文件或生成替代稿。
- 用户指定文件时写入指定文件；未指定时优先直接交付，不自行建立文档体系。
- 文案修改可能使已有图片不同步时，说明影响；未获当前授权时不自动重做图片。
- 不把某一次成功的标题、比喻、篇章结构或页面形式固化为默认模板。
