description: Workspace AGENT baseline behavior without explicit Skill request

prompts:
  - file://prompts/agent-baseline-generation.txt

providers:
  - id: openai:codex-sdk
    label: workspace-agent
    config:
      sandbox_mode: read-only
      approval_policy: never
      skip_git_repo_check: true
      model_reasoning_effort: medium
      # XHS_EVAL_MODEL

tests: file://tests/agent-baseline-generation.yaml
