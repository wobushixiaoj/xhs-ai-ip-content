description: R02/R03 production-skill behavior

prompts:
  - file://prompts/reading-dependency-generation.txt

providers:
  - id: openai:codex-sdk
    label: content-generator
    config:
      sandbox_mode: read-only
      approval_policy: never
      skip_git_repo_check: true
      model_reasoning_effort: medium
      # XHS_EVAL_MODEL

tests: file://tests/reading-dependency-generation.yaml
