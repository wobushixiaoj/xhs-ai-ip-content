description: R02/R03 reading dependency judge calibration

prompts:
  - file://prompts/reading-dependency-judge.txt

providers:
  - id: openai:codex-sdk
    label: reading-dependency-judge
    config:
      sandbox_mode: read-only
      approval_policy: never
      skip_git_repo_check: true
      # XHS_EVAL_MODEL

tests: file://tests/reading-dependency-fixtures.yaml
