description: R04 carousel prompt production-skill behavior

prompts:
  - file://prompts/carousel-prompt-generation.txt

providers:
  - id: openai:codex-sdk
    label: carousel-prompt-generator
    config:
      sandbox_mode: read-only
      approval_policy: never
      skip_git_repo_check: true
      model_reasoning_effort: medium
      # XHS_EVAL_MODEL

tests: file://tests/carousel-prompt-generation.yaml
