请先完整读取当前目录上一级的 `SKILL.md` 和 `references/carousel-prompts.md`，将它们作为本次任务的绑定约束。

以下逐页可见文案已经定稿，不得改写、缩短、增补或重排其中的文字关系：

{{approved_pages}}

用户请求：
{{user_request}}

按“第 N 页 → 页面可见内容 → imagegen 提示词”输出。每页提示词中先设置“必须逐字显示的文字”区，直接复制该页可见文字，不额外套引号或替换任何标点；完成后逐字符核对。不要解释取舍、不要输出 HTML、PNG、CSS 或写作过程。
