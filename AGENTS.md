# xhs-ai-ip-content 源仓库

## 职责边界

此仓库是 `xhs-ai-ip-content`、其图文提示词分支和 Eval 的唯一源头，也是 Codex 当前实际加载的运行目录。

Obsidian 工作区的 `AGENTS.md` 是账号立场、外部内容底线和路由的唯一源头。不要在本仓库复制或镜像那份文件；本仓库只保存完成具体内容任务所需的方法，以及验证这些方法和工作区基线的案例与工具。

## 维护规则

- 普通内容生产、改稿和 Review 不运行 Eval。
- 修改 `SKILL.md`、`references/`、Eval 提示、脚本或测试用例后，运行 Skill 回归；修改 Obsidian 工作区 `AGENTS.md` 时，运行完整回归。
- 完整回归读取 `evals/.env.local` 中配置的真实工作区路径，并以该路径的实际 `AGENTS.md` 运行集成测试；该本地配置不提交。
- 新规则只解决已经出现的真实问题；接纳前至少增加一个应失败反例和一个应通过对照。
- 任一既有案例失败时，不提交或发布本次修改。
- Eval 用于验证维护结果，不得变成日常内容生产的预检步骤或读者可见流程。
